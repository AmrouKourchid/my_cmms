String? validateEmail(String? value) {
  // Always return null to avoid displaying error messages in the form
  return null;
}

String? validatePassword(String? value) {
  // Always return null to avoid displaying error messages in the form
  return null;
}
